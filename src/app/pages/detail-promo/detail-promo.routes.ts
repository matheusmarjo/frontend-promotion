import { Routes } from '@angular/router';
import { DetailPromoComponent } from './detail-promo.component';

export const DETAILPROMO_ROUTES: Routes = [
  { path: ':id', component: DetailPromoComponent },
];
